# 快速开始指南

## 🚀 5 分钟快速集成

### 步骤 1: 安装 Pod

在项目根目录创建或编辑 `Podfile`：

```ruby
platform :ios, '13.0'

target 'YourApp' do
  use_frameworks!
  
  # 本地开发
  pod 'AJVIPLaunchScreen', :path => './AJVIPLaunchScreen'
  
  # 或从 Git 仓库安装（发布后）
  # pod 'AJVIPLaunchScreen', :git => 'https://github.com/ChinaArJun/aj_vip_launch_screen.git'
end
```

运行安装命令：

```bash
cd /path/to/your/project
pod install
```

**重要：** 之后请使用 `.xcworkspace` 文件打开项目！

---

### 步骤 2: 导入框架

在 `SceneDelegate.swift` 文件顶部导入：

```swift
import UIKit
import AJVIPLaunchScreen  // 添加这一行
```

---

### 步骤 3: 添加代理协议

让 SceneDelegate 遵循代理协议：

```swift
class SceneDelegate: UIResponder, UIWindowSceneDelegate, AJLaunchScreenDelegate {
    // ...
}
```

---

### 步骤 4: 显示启动屏幕

在 `scene(_:willConnectTo:options:)` 方法中添加代码：

```swift
func scene(_ scene: UIScene, willConnectTo session: UISceneSession, 
           options connectionOptions: UIScene.ConnectionOptions) {
    guard let windowScene = (scene as? UIWindowScene) else { return }
    
    let window = UIWindow(windowScene: windowScene)
    self.window = window
    
    // 创建配置（选择一个风格）
    var configuration = AJLaunchScreenConfiguration(style: .balloon)
    // 或使用 METRO X 风格
    // var configuration = AJLaunchScreenConfiguration(style: .metroX)
    
    configuration.delegate = self
    
    // 显示启动屏幕
    AJLaunchScreenManager.shared.present(in: window, configuration: configuration)
}
```

---

### 步骤 5: 实现代理方法

在 SceneDelegate 底部添加扩展：

```swift
// MARK: - AJLaunchScreenDelegate
extension SceneDelegate: AJLaunchScreenDelegate {
    
    func launchScreenDidComplete() {
        // 用户完成引导，显示主应用
        let mainVC = MainViewController()
        window?.rootViewController = mainVC
        window?.makeKeyAndVisible()
    }
    
    func launchScreenDidSkip() {
        // 用户跳过引导
        let mainVC = MainViewController()
        window?.rootViewController = mainVC
        window?.makeKeyAndVisible()
    }
    
    func launchScreenDidSelectSubscription() {
        // 用户选择订阅（可选）
        print("用户点击了订阅")
    }
}
```

---

### 完整示例代码

```swift
import UIKit
import AJVIPLaunchScreen

class SceneDelegate: UIResponder, UIWindowSceneDelegate, AJLaunchScreenDelegate {
    
    var window: UIWindow?
    
    func scene(_ scene: UIScene, willConnectTo session: UISceneSession, 
               options connectionOptions: UIScene.ConnectionOptions) {
        guard let windowScene = (scene as? UIWindowScene) else { return }
        
        let window = UIWindow(windowScene: windowScene)
        self.window = window
        
        var configuration = AJLaunchScreenConfiguration(style: .balloon)
        configuration.delegate = self
        
        AJLaunchScreenManager.shared.present(in: window, configuration: configuration)
    }
    
    // MARK: - AJLaunchScreenDelegate
    
    func launchScreenDidComplete() {
        showMainApp()
    }
    
    func launchScreenDidSkip() {
        showMainApp()
    }
    
    private func showMainApp() {
        let mainVC = MainViewController()
        window?.rootViewController = mainVC
        window?.makeKeyAndVisible()
    }
}
```

---

## ✅ 完成！

运行项目（Command + R），您应该能看到漂亮的启动屏幕了！

---

## 🎨 切换风格

只需修改一行代码即可切换风格：

```swift
// Balloon 风格（紫色气球主题）
var configuration = AJLaunchScreenConfiguration(style: .balloon)

// METRO X 风格（科技主题）
var configuration = AJLaunchScreenConfiguration(style: .metroX)
```

---

## 🔧 常见问题

### Q: 提示找不到模块？
**A:** 确保运行了 `pod install` 并使用 `.xcworkspace` 打开项目

### Q: 启动屏幕不显示？
**A:** 检查是否设置了 `configuration.delegate = self`

### Q: 如何只在首次启动时显示？
**A:** 参考"使用指南.md"中的案例 4

---

## 📚 下一步

- 查看 [使用指南.md](./使用指南.md) 了解更多高级用法
- 查看 [Example/](./Example/) 目录中的完整示例代码
- 阅读 [README.md](./README.md) 了解所有配置选项

---

## 💡 提示

### 推荐配置

**消费类应用：**
```swift
var configuration = AJLaunchScreenConfiguration(style: .balloon)
```

**科技类应用：**
```swift
var configuration = AJLaunchScreenConfiguration(
    style: .metroX,
    primaryColor: UIColor(red: 0/255, green: 242/255, blue: 255/255, alpha: 1.0)
)
```

**自定义内容：**
```swift
let slides = [
    AJSlideData(title: "欢迎", description: "...", iconName: "star.fill", color: .systemBlue)
]
var configuration = AJLaunchScreenConfiguration(style: .metroX, customSlides: slides)
```

---

需要帮助？查看 [使用指南.md](./使用指南.md) 或提交 Issue！
